<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <% if(user){ %>
      <form class="form-inline" action= "/logout" method="POST">
        <h5 class="nav-item nav-link">Logged In As: <%= `${user["email"]}`%></h5>
        <button type="submit" class="btn btn-outline-secondary">Logout</button>
        <% } else{ %>
          <div class="navbar-nav">
          <a class="nav-item nav-link" href="/login">Login</a>
          <a class="nav-item nav-link" href="/register">Register</a>
          </div>
          <!-- <form class="form-inline" action= "/login" method="POST" style = "display: flex; justify-content: left">
            <input class="form-control text-muted" type="text" name="username" value= "Username" style="width: 300px; margin: 1em">
            <button type="submit" class="btn btn-outline-dark">Login</button> -->
          <% } %>
    </form>
  </nav>
</header>